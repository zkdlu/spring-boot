server:
  port: 8081

eureka:
  instance:
    prefer-ip-address: true
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: product
  kafka:
    consumer:
      group-id: myGroup
    bootstrap-servers: localhost:9092

kafka:
  topics:
    test: order